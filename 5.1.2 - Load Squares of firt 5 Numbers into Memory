		AREA PROG13, CODE, READONLY     ; Fibonacci numbers stored in memory
		ENTRY

        MOV     R1, #0
		MOV 	R2, #5

CALL    ADD		R1, R1, #1
        STR     R1, [R3], #4
        ADD     R5, R5, #1
BACK    CMP     R1, R2
        BLS     CALL

GO      B       GO

SQUARE  LDR 	R6, R1	

		END
